import React from 'react';

const SectionHeading = ({ title }: { title: string }) => {
  return <div className="text-2xl font-bold mb-4 text-gray-700">{title}</div>;
};

export default SectionHeading;
